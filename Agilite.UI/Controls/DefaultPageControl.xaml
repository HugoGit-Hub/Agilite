<UserControl x:Class="Agilite.UI.Controls.DefaultPageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Agilite.UI.Controls" xmlns:services="clr-namespace:Agilite.UI.Services" xmlns:models="clr-namespace:Agilite.UI.Models" d:DataContext="{d:DesignInstance Type=models:ContactModelBase}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <ListView ItemsSource="{Binding Contacts}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Contact Id" 
                                    DisplayMemberBinding="{Binding IdContact}"
                                    Width="150"/>
                    <GridViewColumn Header="Contact Archived" 
                                    DisplayMemberBinding="{Binding ArchivedContact}"
                                    Width="150"/>
                    <GridViewColumn Header="Contact Id" 
                                    DisplayMemberBinding="{Binding NameContact}"
                                    Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
