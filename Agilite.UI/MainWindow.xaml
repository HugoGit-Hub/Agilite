<Window x:Class="Agilite.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Agilite.UI.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1800" FontSize="20">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:MenuControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" />

        <controls:SidebarControl Grid.Row="1" Grid.Column="0" />

        <controls:DefaultPageControl Grid.Row="1" Grid.Column="1" />

        <controls:StatusbarControl Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" />
    </Grid>
</Window>
